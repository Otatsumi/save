PSU_2013_minishell2
===================

shell

language: C

description: basic shell. execute simple command and execute command "|" , ";" , ">" "<" ">>" and "<<" only. 

to do
=====

manage env -i bash
